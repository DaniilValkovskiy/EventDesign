
  # EventDesign

  This is a code bundle for EventDesign. The original project is available at https://www.figma.com/design/ecz1nAdq08InBV2MspZLar/EventDesign.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  